<h1>Elder Care System</h1>

<p>

1. Server setup create venv with opencv, numpy and ensure is running on Python 3.12 (Will not work any other way).

2. Pi setup setup venv with speicifc with system packages using (python3 -m venv --system-site-packages ~/myenv) and install opencv and numpy using pip.

3. Ensure faces in trainer.yml match names in server.py and pi_local.py.

</p>